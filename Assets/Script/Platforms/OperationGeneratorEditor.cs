using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

/*
[CustomEditor(typeof(OperationGenerator))]
public class OperationGeneratorEditor : Editor
{
    public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        OperationGenerator generator = (OperationGenerator)target;
        if (GUILayout.Button("Generate"))
        {
            generator.initialize();
        }
    }
}
*/